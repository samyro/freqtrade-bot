{
  "_comment": "TEMPLATE — never commit a populated copy. The real version is created by scripts/setup-freqtrade-vps.sh (or scripts/create-secret.sh) into the k8s Secret 'freqtrade-secrets' and mounted at /freqtrade/user_data/config.secrets.json inside the pod.",

  "exchange": {
    "key": "YOUR_KRAKEN_API_KEY",
    "secret": "YOUR_KRAKEN_API_SECRET"
  },

  "api_server": {
    "username": "freqtrader",
    "password": "CHANGE_ME_LONG_RANDOM_STRING",
    "jwt_secret_key": "CHANGE_ME_64_BYTES_HEX",
    "ws_token": "CHANGE_ME_OPTIONAL_WEBSOCKET_TOKEN"
  }
}
